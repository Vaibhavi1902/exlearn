ssh-keygen -t ed25519 -C "your_email@example.com"

eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519  (instead if id I used /.ssh/y) -> i got 2 files named y and y.pub so this become /.ssh/y

cat ~/.ssh/y.pub

ssh -T git@github.com


